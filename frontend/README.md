# Folio Frontend

This folder contains the React and Vite frontend for Folio.

## Commands

```powershell
npm install
npm run dev
npm run build
npm run preview
```

The production build is emitted to `frontend/dist` and is served by the FastAPI backend in the desktop-style workflow.
