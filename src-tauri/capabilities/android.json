{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "android",
  "description": "Native permissions for the Folio Android webview.",
  "windows": ["main"],
  "platforms": ["android"],
  "permissions": [
    "core:default",
    "mobile-runtime:default"
  ]
}
