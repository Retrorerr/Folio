# Aggregated backend requirements. This default set is CPU-safe for ordinary
# Windows PCs. GPU acceleration is optional; install requirements-gpu.txt only
# on machines with a known-good CUDA/ONNXRuntime setup.

-r requirements-base.txt
-r requirements-chatterbox.txt
