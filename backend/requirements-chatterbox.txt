# Chatterbox Turbo ONNX runtime deps.
#
# These are deliberately resolver-friendly. Do not add chatterbox-tts here:
# that package hard-pins a conflicting PyTorch stack and is not used by Folio's
# ONNXRuntime adapter.

onnxruntime-gpu
huggingface_hub
transformers==4.56.2
tokenizers>=0.22.0,<=0.23.0
numpy==2.2.6
librosa
soundfile
resemble-perth
tqdm
