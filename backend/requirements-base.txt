# Base backend dependencies. Installable in one step.
#
# Default installs must work on ordinary Windows PCs without CUDA drivers or
# giant GPU wheels. Folio uses ONNXRuntime CPU by default; machines with a
# known-good GPU runtime can install a GPU provider separately.

numpy==2.2.6
kokoro-onnx==0.4.7
transformers==4.56.2
tokenizers>=0.22.0,<=0.23.0
onnxruntime==1.20.1
huggingface_hub
fastapi
uvicorn
orjson
python-multipart
pillow
soundfile
librosa
tqdm
resemble-perth
ebooklib
beautifulsoup4
lxml
psutil
